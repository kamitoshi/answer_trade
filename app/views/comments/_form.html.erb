<%= form_for @comment, url: video_comments_path(video), remote: true do |f| %>
  <%= f.text_area :detail, class: "form-control", placeholder: "コメントを入力" %>
  <%= f.submit "コメントする", class: "btn btn-outline-primary ma-t-20", id: "comment-submit" %>
<% end %>